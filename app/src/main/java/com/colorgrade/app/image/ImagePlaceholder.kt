package com.colorgrade.app.image

/**
 * Paket [image] berisi logika pemrosesan Bitmap.
 *
 * Implementasi nyata: [PhotoManager] memuat foto dengan ukuran terkendali
 * (anti-boros RAM) dan memperbaiki orientasi EXIF.
 */
object ImagePlaceholder {
    const val DESCRIPTION =
        "Logika pemuatan bitmap tersedia di PhotoManager."
}
